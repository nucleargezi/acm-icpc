#include "MeIoN_Lib/Z_H/MeioN.hpp"
#include "MeIoN_Lib/MeIoN_all.hpp"

// #define tests
void Yorisou() {
  LL(x, y);
  if (not x or not y) iroha UL("black");
  ll r = std::sqrt(x * x + y * y);
  UL((r * r == x * x + y * y or not(r + (x * y < 0) & 1)) ? "black" : "white");
}
#include "MeIoN_Lib/Z_H/main.hpp"