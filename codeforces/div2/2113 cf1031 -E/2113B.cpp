#include "MeIoN_Lib/Z_H/MeioN.hpp"
#include "MeIoN_Lib/MeIoN_all.hpp"

#define tests
void Yorisou() {
  LL(w, h, a, b);
  LL(x1, y1, x2, y2);
  if (ABS(x2 - x1) % a == 0 and ABS(x2 - x1)) iroha Yes();
  if (ABS(y2 - y1) % b == 0 and ABS(y2 - y1)) iroha Yes();
  No();
}
#include "MeIoN_Lib/Z_H/main.hpp" 