#include "MeIoN_Lib/Z_H/MeioN.hpp"
#include "MeIoN_Lib/MeIoN_all.hpp"

#define tests
void Yorisou() {
  LL(n);
  vector<T3<int>> ans;
  meion ad = [&](int i, int l, int r) {
    if (l >= r) iroha;
    ans.emplace_back(i + 1, l + 1, r + 1);
  };
  FOR(i, 0, n) {
    ad(i, 0, i);
    ad(i, i + 1, n - 1);
  }
  UL(len(ans));
  for (meion [x, y, z] : ans) {
    UL(x, y, z);
  }
}
#include "MeIoN_Lib/Z_H/main.hpp"