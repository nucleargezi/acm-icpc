#include "MeIoN_Lib/Z_H/MeioN.hpp"
#include "MeIoN_Lib/MeIoN_all.hpp"

// #define tests
bool f(ll x) {
  ll y = std::sqrt(x);
  iroha y * y == x;
}
void Yorisou() {
  LL(n);
  ll m = 0, x = n;
  while (x) m += x % 10, x /= 10;
  Yes(f(m) and f(n));
}
#include "MeIoN_Lib/Z_H/main.hpp"